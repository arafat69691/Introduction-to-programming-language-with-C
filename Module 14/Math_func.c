#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main(){
    int c =5.5;
    int a= ceil(c);
    int d = 5.5;
    int b = floor(d);
    int n = 45.33;
    int k = round(k);
    int t= 45;
    int sqrt1=sqrt(t);
    int r=5,s=5;
    int p = pow(r,s);
    int g=-44;
    int abs1=abs(g);
    printf("%d %d %d %d %d ",a,b,k,sqrt1,p,abs1);
    return 0;
}